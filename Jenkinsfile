pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // Execute build commands, such as compiling the code
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
                // Execute testing commands, such as running unit tests
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying...'
                // Execute deployment commands, such as deploying code to the server
            }
        }
    }
}
